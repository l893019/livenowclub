export const metadata = {
  title: "Connect | The Live Now Club",
  description: "Share your story or reach out. A space for real connection.",
};

export default function ConnectLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
